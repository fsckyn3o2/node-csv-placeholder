rowIndex=${helper.getDataKey()}
pid=bull${helper.get('mynum')}
ppid=${helper.format("10${mynum}") + 1}
ppidGeek=${helper.math("10${mynum}").add(1).done()}
valueAfterTrim=${helper.trim(' this   is   a   test  ')}
valueAfterSwitch=${helper.switch( "${mynum}", ["252", "001"], ["${header2}", "${header3}"] )}
